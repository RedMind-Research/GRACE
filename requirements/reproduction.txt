# Lock input for the repository-only Tau² Telecom reproduction. The editable
# VCS install is intentional: the pinned upstream revision keeps benchmark data
# in its source tree instead of packaging those files in a wheel.
-e git+https://github.com/sierra-research/tau2-bench.git@c5b2d228d850c59b749b93cf32c4745d3aa53967#egg=tau2

# Locked model-facing runtime for the released reproduction path.
litellm[google]==1.92.0
openai==2.21.0
google-auth==2.48.0
google-cloud-aiplatform==1.133.0
google-genai==1.65.0
